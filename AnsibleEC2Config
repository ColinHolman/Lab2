# commands used on the EC2 controller instance to set up ansible

sudo apt-get update
sudo apt-add-repository -y ppa:ansible/ansible
sudo apt-get install -y ansible
sudo apt install python3-pip
sudo pip install boto boto3